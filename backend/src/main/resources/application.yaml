spring:
# JPA 설정
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

# 최대 이미지 파일 크기 설정
  servlet:
    multipart:
      maxFileSize: 10MB
      maxRequestSize: 10MB